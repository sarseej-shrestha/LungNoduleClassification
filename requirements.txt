# Core numerical / data
numpy>=1.26,<2.0
pandas>=2.0

# Imaging
SimpleITK>=2.3
opencv-python>=4.8
pydicom>=2.4
pylidc>=0.2.3          # LIDC-IDRI annotation parsing (XML consensus, malignancy ratings)
setuptools<81          # pylidc is unmaintained and imports pkg_resources, removed in setuptools>=81

# Deep learning
torch>=2.2
torchvision>=0.17

# Classical ML / metrics / stats
scikit-learn>=1.3
scipy>=1.11

# Visualization
matplotlib>=3.8
